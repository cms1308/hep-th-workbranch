{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && git add -A && { git diff --cached --quiet || git commit -q -m \"Auto-commit: harness update\"; } && git push -q 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Auto-committing and pushing repo changes"
          }
        ]
      }
    ]
  }
}
